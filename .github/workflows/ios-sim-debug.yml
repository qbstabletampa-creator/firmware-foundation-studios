# iOS simulator DEBUG launch: same Release build as ios-sim-smoke, but launches
# the app with --console-pty AND streams the simulator os_log, so the actual JS
# startup error (which expo-updates ErrorRecovery swallows into a bare SIGABRT,
# leaving the .ips crash report with no reason string) is captured to an artifact.
# One-off diagnostic for the 2026-06-13 universal launch crash. Not a gate.
#
# Trigger: gh workflow run ios-sim-debug -f app=<dir>
name: ios-sim-debug

on:
  workflow_dispatch:
    inputs:
      app:
        description: "App dir under archive/apps"
        required: true
        default: "manna-catch"

jobs:
  debug:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: archive/package-lock.json

      - name: Install workspace deps
        working-directory: archive
        run: npm ci --no-audit --no-fund

      - name: Install standalone app deps
        working-directory: archive/apps/${{ github.event.inputs.app }}
        run: if [ -f package-lock.json ]; then npm ci --no-audit --no-fund; fi

      - name: Prebuild native iOS project
        working-directory: archive/apps/${{ github.event.inputs.app }}
        env:
          EXPO_NO_DEPENDENCY_VALIDATION: "1"
        run: npx expo prebuild --platform ios --no-install

      - name: Select newest Xcode for SDK 56
        if: ${{ github.event.inputs.app == 'gosple' }}
        run: |
          XC=$(ls -d /Applications/Xcode_*.app | sort -V | tail -1)
          sudo xcode-select -s "$XC"
          xcodebuild -version

      - name: pod install
        working-directory: archive/apps/${{ github.event.inputs.app }}/ios
        run: pod install

      - name: Build Release for simulator
        working-directory: archive/apps/${{ github.event.inputs.app }}/ios
        run: |
          set -o pipefail
          WORKSPACE=$(ls -d *.xcworkspace | head -1)
          SCHEME="${WORKSPACE%.xcworkspace}"
          xcodebuild -workspace "$WORKSPACE" -scheme "$SCHEME" \
            -configuration Release -sdk iphonesimulator \
            -derivedDataPath build CODE_SIGNING_ALLOWED=NO \
            -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -20

      - name: Launch and capture the app's JS error
        working-directory: archive/apps/${{ github.event.inputs.app }}/ios
        run: |
          set -x
          APP_PATH=$(ls -d build/Build/Products/Release-iphonesimulator/*.app | head -1)
          BUNDLE_ID=$(/usr/libexec/PlistBuddy -c 'Print CFBundleIdentifier' "$APP_PATH/Info.plist")
          PROC=$(basename "$APP_PATH" .app)
          DEVICE=$(xcrun simctl list devices available -j | python3 -c "import json,sys; d=json.load(sys.stdin)['devices']; print(next(dev['udid'] for k in d for dev in d[k] if 'iPhone' in dev['name']))")
          echo "app=$APP_PATH bundle=$BUNDLE_ID proc=$PROC device=$DEVICE"
          xcrun simctl boot "$DEVICE" || true
          xcrun simctl bootstatus "$DEVICE" -b
          xcrun simctl install "$DEVICE" "$APP_PATH"
          # fire and forget — simctl launch returns immediately with a PID; the app crashes ~1.2s in
          xcrun simctl launch "$DEVICE" "$BUNDLE_ID" || true
          # give it time to start, throw, and let expo-updates ErrorRecovery log + abort
          sleep 15
          # dump ONLY this app process's log (tiny + exact). RN logs the JS error here before the abort.
          xcrun simctl spawn "$DEVICE" log show --last 3m --info --debug --style syslog --predicate "process == \"$PROC\"" > app-oslog.txt 2>&1 || true
          echo "app-oslog lines: $(wc -l < app-oslog.txt)"
          echo "########## FULL APP LOG (process == $PROC) ##########"
          cat app-oslog.txt
          echo "########## ERROR-FOCUSED ##########"
          grep -iE "Exception|fatal|Unhandled|RCTFatal|facebook::react|reanimated|worklet|skia|TurboModule|invariant|RuntimeError|undefined|is not a function|cannot read|Error|ErrorRecovery|abort|throw|require|Unable to|Cannot find" app-oslog.txt | head -120 || echo "no candidate lines matched"

      - name: Upload app log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-logs-${{ github.event.inputs.app }}
          path: archive/apps/${{ github.event.inputs.app }}/ios/app-oslog.txt
          if-no-files-found: warn
